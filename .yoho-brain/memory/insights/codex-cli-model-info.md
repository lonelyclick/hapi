## Codex CLI/助手模型信息可见性

- 在当前 Codex CLI 这类代理运行环境里，助手侧通常拿不到底层 `model` 的精确标识（例如具体是 `gpt-4.1` 还是其它变体）。
- 想确认实际使用的模型：优先从调用端/网关的日志、UI 的会话详情、或 API 请求参数里的 `model` 字段查看。
- 我能稳定提供的自检信息：知识截止时间、当前会话日期，以及我在本会话中可用的工具能力。
